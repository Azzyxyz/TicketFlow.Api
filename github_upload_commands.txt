# Komendy do wrzucenia projektu na GitHub
# 1. Utwórz puste repozytorium na GitHub, np. TicketFlow.Api
# 2. W terminalu przejdź do katalogu projektu i wykonaj:

git init
git add .
git commit -m "Initial commit - TicketFlow API"
git branch -M main
git remote add origin https://github.com/TWOJ_LOGIN/TicketFlow.Api.git
git push -u origin main

# Jeżeli używasz GitHub CLI:
# gh repo create TicketFlow.Api --public --source=. --remote=origin --push
